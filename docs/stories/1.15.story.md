# Story 1.15 — Painel Admin de Gerenciamento de Usuários

**Status:** Ready for Review
**Epic:** 1 — IcognitoChat MVP
**Story:** 1.15
**Depende de:** 1.2, 1.10

---

## Descrição

Como administrador, quero visualizar todos os usuários cadastrados, ver quando cada um foi visto pela última vez, e poder remover contas, para que eu tenha controle sobre quem pode acessar o sistema.

---

## Contexto Técnico

Hoje o admin só consegue criar usuários via `POST /api/auth/admin/users`. Não há listagem, não há exclusão, não há visualização de atividade. Com o banco persistindo, usuários se acumulam sem controle.

A UI pode ser uma seção na tela de login (visível apenas quando autenticado como admin) ou uma rota dedicada `/admin`.

---

## Critérios de Aceitação

- [ ] Rota `GET /api/auth/admin/users` retorna lista de usuários com `{ id, username, created_at, last_seen_at }` (sem `password_hash`)
- [ ] Rota `DELETE /api/auth/admin/users/[id]` remove um usuário pelo ID (requer autenticação admin via `MASTER_PASSWORD`)
- [ ] Página `/admin` (protegida por `MASTER_PASSWORD` via sessão admin separada ou query param) exibe:
  - Tabela de usuários com username, data de criação, último acesso
  - Botão de remover por usuário (com confirmação)
  - Formulário de criação de novo usuário (já existente, migrar para cá)
- [ ] Admin não pode remover a si mesmo se houver sessão ativa
- [ ] Após remoção, usuário removido é desconectado do Socket.io se estiver online

---

## Tarefas

- [x] Atualizar `src/app/api/auth/admin/users/route.ts`:
  - `GET`: listar todos os usuários (sem password_hash), ordenados por `last_seen_at DESC`
  - Manter `POST` existente para criação

- [x] Criar `src/app/api/auth/admin/users/[id]/route.ts`:
  - `DELETE`: validar `MASTER_PASSWORD` no header ou sessão, deletar usuário pelo ID

- [x] Criar `src/app/admin/page.tsx`:
  - Formulário de autenticação admin (senha mestra)
  - Após autenticar: exibir tabela de usuários + formulário de criação
  - Botão "Remover" por linha com `window.confirm`

- [x] Atualizar `server.ts`:
  - `userSockets: Map<username, Set<socketId>>` exposto no global para API routes
  - `force-logout` emitido pelo `DELETE` handler para sockets do usuário removido

---

## File List

- `src/app/api/auth/admin/users/route.ts`
- `src/app/api/auth/admin/users/[id]/route.ts` *(novo)*
- `src/app/admin/page.tsx` *(novo)*
- `server.ts`
- `src/types/global.d.ts`

---

## Dev Agent Record

**Agent Model Used:** claude-sonnet-4-6

### Completion Notes

- `admin/users/route.ts`: `GET` adicionado com validação de `x-master-password`; query ordena por `last_seen_at DESC NULLS LAST`
- `admin/users/[id]/route.ts`: `DELETE` busca username antes de deletar (necessário para emitir `force-logout`); retorna 404 se usuário não existe
- `server.ts`: `userSockets: Map<username, Set<socketId>>` criado e exposto em `global.userSockets`; populado no `join-room`, limpo no `disconnect`
- `admin/page.tsx`: autenticação via senha mestra em estado local; tabela com criação + remoção; credenciais de novo usuário exibidas uma única vez em banner verde
- Nota AC "admin não pode remover a si mesmo": admin usa MASTER_PASSWORD, não sessão de usuário — proteção não se aplica neste fluxo (admin pode não ser um usuário registrado)
- Fix QA HIGH: `adminLimiter` aplicado também ao `GET` com `recordFailure` em senha incorreta
- Fix QA HIGH: listener `force-logout` adicionado em `socket-client.ts` — chama `POST /api/auth/logout` e redireciona para `/` via `window.location.href`

### Change Log

| Data | Mudança |
|------|---------|
| 2026-03-21 | Implementação completa da Story 1.15 — Painel Admin de Gerenciamento de Usuários |
| 2026-03-21 | Fix QA: rate limiting no GET + listener force-logout no cliente |
