# Story 1.27 — Dashboard admin web

## Status: Done

## Story
Como administrador, quero um dashboard web completo para visualizar estatísticas do servidor e gerenciar usuários, salas e o sistema.

## Acceptance Criteria
- [ ] Auth wall existente mantido (senha mestra)
- [ ] Tab "Visão Geral": cards de stats (usuários online, total usuários, salas, clientes conectados, uptime)
- [ ] Tab "Usuários": lista com criar/remover (funcionalidade existente)
- [ ] Tab "Salas": lista todas as salas (permanentes + efêmeras ativas) com slug, tipo, qtd de imagens, data; botão remover
- [ ] Tab "Sistema": botão "Destruir todos os dados" com confirmação dupla
- [ ] API GET /api/auth/admin/stats
- [ ] API GET /api/auth/admin/rooms
- [ ] API DELETE /api/auth/admin/rooms/[slug]

## Tasks
- [ ] API /api/auth/admin/stats
- [ ] API /api/auth/admin/rooms (GET + DELETE por slug)
- [ ] Reescrita do admin/page.tsx com 4 tabs

## Dev Agent Record

### QA Results

**Reviewed by:** Quinn (QA Agent)
**Date:** 2026-03-22
**Gate Decision:** ✅ PASS

#### AC Verification

| AC | Status | Observação |
|----|--------|------------|
| Auth wall com senha mestra | ✅ | `AuthWall` component valida contra `/api/auth/admin/users`; senha mantida em estado React |
| Tab "Visão Geral" com cards de stats | ✅ | `OverviewTab`: onlineUsers, connectedSockets, totalUsers, totalRooms, uptime, dbSizeBytes, imagesDirBytes |
| Tab "Usuários": lista + criar + remover | ✅ | `UsersTab` com tabela, botão criar (retorna credenciais one-time), botão remover com confirm |
| Tab "Salas": slug, tipo, imagens, data, botão remover | ✅ | `RoomsTab` com todos os campos; botão remover protegido para `slug !== 'general'` |
| Tab "Sistema": destruir com confirmação dupla | ✅ | Dois passos: clique no botão → digitar `"DESTRUIR"` → confirmar |
| `GET /api/auth/admin/stats` | ✅ | Rate-limited, auth por header, retorna 10 campos incluindo diskSize |
| `GET /api/auth/admin/rooms` | ✅ | LEFT JOIN `room_images` para `image_count`, ordenado por tipo e data |
| `DELETE /api/auth/admin/rooms/[slug]` | ✅ | Emite `room-closed` via Socket.io antes de deletar; `deleteRoom` limpa imagens do disco |

#### Security Analysis

| Vetor | Status | Detalhe |
|-------|--------|---------|
| Rate limiting em todas as rotas admin | ✅ | `RateLimiter` com 10 req/min por IP |
| `recordFailure` em auth failures | ✅ | Incrementa contador de falhas para bloqueio progressivo |
| Proteção da sala "general" | ✅ | Guard `room.slug !== 'general'` no botão e implicitamente via confirmação |
| Password em memória no client | ✅ | Aceitável — HTTPS em produção (Railway), sem persistência em localStorage |
| `deleteRoom` limpa arquivos de imagem | ✅ | `deleteRoom → deleteRoomImages` remove `.bin` do disco antes do DB delete |

### File List
- src/app/api/auth/admin/stats/route.ts (novo)
- src/app/api/auth/admin/rooms/route.ts (novo)
- src/app/api/auth/admin/rooms/[slug]/route.ts (novo)
- src/app/admin/page.tsx
