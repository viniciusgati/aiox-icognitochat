# Story 1.1 — Setup do Projeto & Infraestrutura Base

**Status:** Done
**Epic:** 1 — IcognitoChat MVP
**Story:** 1.1

---

## Descrição

Como desenvolvedor, quero inicializar o projeto Next.js com todas as dependências necessárias (Socket.io, SQLite, next-pwa, Tailwind) e configurar o servidor customizado para suportar WebSocket, para que as demais histórias possam ser implementadas.

---

## Critérios de Aceitação

- [ ] Projeto Next.js 14 inicializado com TypeScript
- [ ] Tailwind CSS configurado
- [ ] next-pwa configurado com manifest.json básico
- [ ] SQLite (better-sqlite3) instalado e db.ts criado com schema inicial (users, rooms)
- [ ] Socket.io instalado (servidor + cliente)
- [ ] Servidor customizado (`server.ts`) que integra Next.js + Socket.io
- [ ] iron-session instalado e configurado para sessões JWT
- [ ] Scripts no package.json: `dev`, `build`, `start`
- [ ] `.env.example` com variáveis necessárias
- [ ] Projeto roda localmente com `npm run dev` sem erros

---

## Tarefas

- [x] `npx create-next-app` com TypeScript + Tailwind
- [x] Instalar dependências: `socket.io`, `socket.io-client`, `better-sqlite3`, `@types/better-sqlite3`, `iron-session`, `next-pwa`, `bcryptjs`, `@types/bcryptjs`
- [x] Criar `src/lib/db.ts` com conexão SQLite e migrations iniciais
- [x] Criar `server.ts` (custom server) com Socket.io
- [x] Criar `public/manifest.json` básico
- [x] Configurar `next.config.js` com next-pwa
- [x] Criar `.env.example` com `MASTER_PASSWORD`, `SESSION_SECRET`
- [x] Testar: `npm run dev` sobe sem erros

---

## File List

- `package.json` — dependências e scripts npm
- `tsconfig.json` — configuração TypeScript para Next.js
- `tsconfig.server.json` — configuração TypeScript para server.ts (CommonJS)
- `next.config.js` — Next.js + next-pwa configurado
- `tailwind.config.ts` — Tailwind CSS configurado
- `postcss.config.js` — PostCSS para Tailwind
- `.eslintrc.json` — ESLint com preset Next.js
- `next-env.d.ts` — tipos globais Next.js
- `server.ts` — servidor customizado Next.js + Socket.io
- `src/lib/db.ts` — conexão SQLite com schema inicial (users, rooms, messages)
- `src/app/layout.tsx` — layout raiz com metadata PWA
- `src/app/page.tsx` — página inicial
- `src/app/globals.css` — estilos globais Tailwind
- `public/manifest.json` — PWA manifest
- `public/icon.svg` — ícone SVG fonte
- `public/icon-192.png` — ícone PWA 192x192
- `public/icon-512.png` — ícone PWA 512x512
- `src/lib/session.ts` — configuração iron-session (SessionOptions, IronSessionData)
- `src/types/global.d.ts` — declaração global tipada para Socket.io
- `.env.example` — variáveis de ambiente (MASTER_PASSWORD, SESSION_SECRET, PORT)
- `.gitignore` — atualizado com padrões Next.js e SQLite

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

## QA Results

### Gate Decision: CONCERNS
**Revisor:** Quinn (QA Agent) | **Data:** 2026-03-19

#### Problemas a Corrigir
1. **[HIGH]** `iron-session` não configurado — criar `src/lib/session.ts` com `SessionOptions` e tipo `IronSessionData`. AC diz "instalado e **configurado**".
2. **[MEDIUM]** Ícones PWA ausentes — `public/icon-192.png` e `public/icon-512.png` referenciados no `manifest.json` mas não existem. Criar placeholders ou SVGs convertidos.
3. **[LOW]** `(global as any).io` em `server.ts:27` — declarar tipo global para `io` em `src/types/global.d.ts`.

#### O que passou ✅
- Next.js 14.2.35 + TypeScript + Tailwind configurados
- SQLite com WAL mode, foreign keys, schema completo
- Socket.io integrado funcionalmente
- next-pwa configurado
- Scripts npm corretos
- `.env.example` completo
- `npm run dev/build/lint/typecheck` todos passando

### Gate Decision: PASS (2ª revisão)
**Revisor:** Quinn (QA Agent) | **Data:** 2026-03-19

Todos os 3 fixes aplicados corretamente:
1. ✅ `src/lib/session.ts` — iron-session configurado com SessionOptions, SessionData e module augmentation
2. ✅ `public/icon-192.png` + `icon-512.png` — ícones PWA válidos (gerados de SVG)
3. ✅ `src/types/global.d.ts` — `global.io` tipado como `SocketIOServer | undefined`

**Story aprovada para merge.**

---

### Completion Notes
- Next.js configurado manualmente (create-next-app não aceita diretório com arquivos existentes)
- next-pwa desabilitado em dev por padrão (ativado apenas em production)
- Next.js atualizado para 14.2.35 (patch de segurança vs 14.2.29 original)
- Vulnerabilidades residuais são transitivas de next-pwa@5 e eslint — não corrigíveis sem breaking changes
- SQLite com WAL mode e foreign keys habilitados
- `npm run dev` ✅ | `npm run build` ✅ | `npm run lint` ✅ | `npm run typecheck` ✅
